import 'package:flutter/material.dart';

const primaryColor = Color(0XFF03346E);
const lightColor = Colors.white;
const darkColor = Colors.black;
const shadowColor = Colors.grey;
const transparent = Colors.transparent;
